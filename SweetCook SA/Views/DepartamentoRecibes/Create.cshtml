@model SweetCook_SA.Models.DepartamentoRecibe

@{
    ViewBag.Title = "Crear";
}

<style>
    .Contenedor {
        margin-top: 15%;
        width: 100vw 100vh;
        height: 50vh 150vw;
        position: relative;
        background: rgb(255, 255, 255, .6);
        box-sizing: border-box;
        box-shadow: 0 15px 25px rgba(0,0,0,.9);
        border-radius: 10px;
    }
</style>

<div class="Contenedor col-lg-12 col-lg-10">
    <h2 style="text-align:center; font-size:50px; font-weight:bold;">Nuevo</h2>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            <h4 style="font-size:25px;font-weight:bold;">Departamento que Recibe</h4>
            <hr />
            <left>
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(model => model.NombreDepartamentoRecibe, htmlAttributes: new { @class = "control-label col-md-2" })
                    <div style="margin-left:.1vh" class="col-md-10">
                        @Html.EditorFor(model => model.NombreDepartamentoRecibe, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.NombreDepartamentoRecibe, "", new { @class = "text-danger" })
                    </div>
                </div>
            </left>

            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <left>
                        <input type="submit" value="Crear" class="btn btn-success" />
                        <p class="btn btn-info">
                            @Html.ActionLink("Regresar", "Index")
                        </p>
                    </left>
                </div>
            </div>
        </div>
    }
    <br>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
